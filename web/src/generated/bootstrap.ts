// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v3.19.1
// source: bootstrap.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "relay.bootstrap";

export interface UserCard {
  userId: string;
  username: string;
  displayName: string;
  avatarUrl?: string | undefined;
}

export interface AppWorkspaceItem {
  workspaceId: string;
  name: string;
  iconUrl?: string | undefined;
  unreadCount: number;
}

export interface GetAppBootstrapRequest {
}

export interface GetAppBootstrapResponse {
  viewer: UserCard | undefined;
  workspaces: AppWorkspaceItem[];
  pendingFriendRequestCount: number;
}

export interface WorkspaceBootstrapHeader {
  workspaceId: string;
  name: string;
  iconUrl?: string | undefined;
  memberCount: number;
  unreadCount: number;
}

export interface WorkspaceBootstrapChannel {
  channelId: string;
  conversationId: string;
  name: string;
  channelKind: string;
  position: number;
  unreadCount: number;
}

export interface GetWorkspaceBootstrapRequest {
  workspaceId: string;
}

export interface GetWorkspaceBootstrapResponse {
  workspace: WorkspaceBootstrapHeader | undefined;
  channels: WorkspaceBootstrapChannel[];
}

export interface DmThreadItem {
  conversationId: string;
  dmPairId: string;
  peerUserId: string;
  peerUsername: string;
  peerDisplayName: string;
  peerAvatarUrl?: string | undefined;
  unreadCount: number;
  lastMessagePreview: string;
  lastActivityAt?: Date | undefined;
}

export interface GetDmBootstrapRequest {
}

export interface GetDmBootstrapResponse {
  items: DmThreadItem[];
}

function createBaseUserCard(): UserCard {
  return { userId: "", username: "", displayName: "", avatarUrl: undefined };
}

export const UserCard: MessageFns<UserCard> = {
  encode(message: UserCard, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.displayName !== "") {
      writer.uint32(26).string(message.displayName);
    }
    if (message.avatarUrl !== undefined) {
      writer.uint32(34).string(message.avatarUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserCard {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserCard();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.avatarUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserCard {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      avatarUrl: isSet(object.avatarUrl)
        ? globalThis.String(object.avatarUrl)
        : isSet(object.avatar_url)
        ? globalThis.String(object.avatar_url)
        : undefined,
    };
  },

  toJSON(message: UserCard): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.avatarUrl !== undefined) {
      obj.avatarUrl = message.avatarUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<UserCard>): UserCard {
    return UserCard.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UserCard>): UserCard {
    const message = createBaseUserCard();
    message.userId = object.userId ?? "";
    message.username = object.username ?? "";
    message.displayName = object.displayName ?? "";
    message.avatarUrl = object.avatarUrl ?? undefined;
    return message;
  },
};

function createBaseAppWorkspaceItem(): AppWorkspaceItem {
  return { workspaceId: "", name: "", iconUrl: undefined, unreadCount: 0 };
}

export const AppWorkspaceItem: MessageFns<AppWorkspaceItem> = {
  encode(message: AppWorkspaceItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspaceId !== "") {
      writer.uint32(10).string(message.workspaceId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.iconUrl !== undefined) {
      writer.uint32(26).string(message.iconUrl);
    }
    if (message.unreadCount !== 0) {
      writer.uint32(32).int32(message.unreadCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppWorkspaceItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppWorkspaceItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.iconUrl = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.unreadCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppWorkspaceItem {
    return {
      workspaceId: isSet(object.workspaceId)
        ? globalThis.String(object.workspaceId)
        : isSet(object.workspace_id)
        ? globalThis.String(object.workspace_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      iconUrl: isSet(object.iconUrl)
        ? globalThis.String(object.iconUrl)
        : isSet(object.icon_url)
        ? globalThis.String(object.icon_url)
        : undefined,
      unreadCount: isSet(object.unreadCount)
        ? globalThis.Number(object.unreadCount)
        : isSet(object.unread_count)
        ? globalThis.Number(object.unread_count)
        : 0,
    };
  },

  toJSON(message: AppWorkspaceItem): unknown {
    const obj: any = {};
    if (message.workspaceId !== "") {
      obj.workspaceId = message.workspaceId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.iconUrl !== undefined) {
      obj.iconUrl = message.iconUrl;
    }
    if (message.unreadCount !== 0) {
      obj.unreadCount = Math.round(message.unreadCount);
    }
    return obj;
  },

  create(base?: DeepPartial<AppWorkspaceItem>): AppWorkspaceItem {
    return AppWorkspaceItem.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AppWorkspaceItem>): AppWorkspaceItem {
    const message = createBaseAppWorkspaceItem();
    message.workspaceId = object.workspaceId ?? "";
    message.name = object.name ?? "";
    message.iconUrl = object.iconUrl ?? undefined;
    message.unreadCount = object.unreadCount ?? 0;
    return message;
  },
};

function createBaseGetAppBootstrapRequest(): GetAppBootstrapRequest {
  return {};
}

export const GetAppBootstrapRequest: MessageFns<GetAppBootstrapRequest> = {
  encode(_: GetAppBootstrapRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAppBootstrapRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAppBootstrapRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetAppBootstrapRequest {
    return {};
  },

  toJSON(_: GetAppBootstrapRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetAppBootstrapRequest>): GetAppBootstrapRequest {
    return GetAppBootstrapRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetAppBootstrapRequest>): GetAppBootstrapRequest {
    const message = createBaseGetAppBootstrapRequest();
    return message;
  },
};

function createBaseGetAppBootstrapResponse(): GetAppBootstrapResponse {
  return { viewer: undefined, workspaces: [], pendingFriendRequestCount: 0 };
}

export const GetAppBootstrapResponse: MessageFns<GetAppBootstrapResponse> = {
  encode(message: GetAppBootstrapResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.viewer !== undefined) {
      UserCard.encode(message.viewer, writer.uint32(10).fork()).join();
    }
    for (const v of message.workspaces) {
      AppWorkspaceItem.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.pendingFriendRequestCount !== 0) {
      writer.uint32(24).int32(message.pendingFriendRequestCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetAppBootstrapResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetAppBootstrapResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.viewer = UserCard.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workspaces.push(AppWorkspaceItem.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.pendingFriendRequestCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetAppBootstrapResponse {
    return {
      viewer: isSet(object.viewer) ? UserCard.fromJSON(object.viewer) : undefined,
      workspaces: globalThis.Array.isArray(object?.workspaces)
        ? object.workspaces.map((e: any) => AppWorkspaceItem.fromJSON(e))
        : [],
      pendingFriendRequestCount: isSet(object.pendingFriendRequestCount)
        ? globalThis.Number(object.pendingFriendRequestCount)
        : isSet(object.pending_friend_request_count)
        ? globalThis.Number(object.pending_friend_request_count)
        : 0,
    };
  },

  toJSON(message: GetAppBootstrapResponse): unknown {
    const obj: any = {};
    if (message.viewer !== undefined) {
      obj.viewer = UserCard.toJSON(message.viewer);
    }
    if (message.workspaces?.length) {
      obj.workspaces = message.workspaces.map((e) => AppWorkspaceItem.toJSON(e));
    }
    if (message.pendingFriendRequestCount !== 0) {
      obj.pendingFriendRequestCount = Math.round(message.pendingFriendRequestCount);
    }
    return obj;
  },

  create(base?: DeepPartial<GetAppBootstrapResponse>): GetAppBootstrapResponse {
    return GetAppBootstrapResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetAppBootstrapResponse>): GetAppBootstrapResponse {
    const message = createBaseGetAppBootstrapResponse();
    message.viewer = (object.viewer !== undefined && object.viewer !== null)
      ? UserCard.fromPartial(object.viewer)
      : undefined;
    message.workspaces = object.workspaces?.map((e) => AppWorkspaceItem.fromPartial(e)) || [];
    message.pendingFriendRequestCount = object.pendingFriendRequestCount ?? 0;
    return message;
  },
};

function createBaseWorkspaceBootstrapHeader(): WorkspaceBootstrapHeader {
  return { workspaceId: "", name: "", iconUrl: undefined, memberCount: 0, unreadCount: 0 };
}

export const WorkspaceBootstrapHeader: MessageFns<WorkspaceBootstrapHeader> = {
  encode(message: WorkspaceBootstrapHeader, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspaceId !== "") {
      writer.uint32(10).string(message.workspaceId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.iconUrl !== undefined) {
      writer.uint32(26).string(message.iconUrl);
    }
    if (message.memberCount !== 0) {
      writer.uint32(32).int32(message.memberCount);
    }
    if (message.unreadCount !== 0) {
      writer.uint32(40).int32(message.unreadCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkspaceBootstrapHeader {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkspaceBootstrapHeader();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.iconUrl = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.memberCount = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.unreadCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkspaceBootstrapHeader {
    return {
      workspaceId: isSet(object.workspaceId)
        ? globalThis.String(object.workspaceId)
        : isSet(object.workspace_id)
        ? globalThis.String(object.workspace_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      iconUrl: isSet(object.iconUrl)
        ? globalThis.String(object.iconUrl)
        : isSet(object.icon_url)
        ? globalThis.String(object.icon_url)
        : undefined,
      memberCount: isSet(object.memberCount)
        ? globalThis.Number(object.memberCount)
        : isSet(object.member_count)
        ? globalThis.Number(object.member_count)
        : 0,
      unreadCount: isSet(object.unreadCount)
        ? globalThis.Number(object.unreadCount)
        : isSet(object.unread_count)
        ? globalThis.Number(object.unread_count)
        : 0,
    };
  },

  toJSON(message: WorkspaceBootstrapHeader): unknown {
    const obj: any = {};
    if (message.workspaceId !== "") {
      obj.workspaceId = message.workspaceId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.iconUrl !== undefined) {
      obj.iconUrl = message.iconUrl;
    }
    if (message.memberCount !== 0) {
      obj.memberCount = Math.round(message.memberCount);
    }
    if (message.unreadCount !== 0) {
      obj.unreadCount = Math.round(message.unreadCount);
    }
    return obj;
  },

  create(base?: DeepPartial<WorkspaceBootstrapHeader>): WorkspaceBootstrapHeader {
    return WorkspaceBootstrapHeader.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WorkspaceBootstrapHeader>): WorkspaceBootstrapHeader {
    const message = createBaseWorkspaceBootstrapHeader();
    message.workspaceId = object.workspaceId ?? "";
    message.name = object.name ?? "";
    message.iconUrl = object.iconUrl ?? undefined;
    message.memberCount = object.memberCount ?? 0;
    message.unreadCount = object.unreadCount ?? 0;
    return message;
  },
};

function createBaseWorkspaceBootstrapChannel(): WorkspaceBootstrapChannel {
  return { channelId: "", conversationId: "", name: "", channelKind: "", position: 0, unreadCount: 0 };
}

export const WorkspaceBootstrapChannel: MessageFns<WorkspaceBootstrapChannel> = {
  encode(message: WorkspaceBootstrapChannel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channelId !== "") {
      writer.uint32(10).string(message.channelId);
    }
    if (message.conversationId !== "") {
      writer.uint32(18).string(message.conversationId);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.channelKind !== "") {
      writer.uint32(34).string(message.channelKind);
    }
    if (message.position !== 0) {
      writer.uint32(40).int32(message.position);
    }
    if (message.unreadCount !== 0) {
      writer.uint32(48).int32(message.unreadCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WorkspaceBootstrapChannel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWorkspaceBootstrapChannel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.channelId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.conversationId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.channelKind = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.position = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.unreadCount = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WorkspaceBootstrapChannel {
    return {
      channelId: isSet(object.channelId)
        ? globalThis.String(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.String(object.channel_id)
        : "",
      conversationId: isSet(object.conversationId)
        ? globalThis.String(object.conversationId)
        : isSet(object.conversation_id)
        ? globalThis.String(object.conversation_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      channelKind: isSet(object.channelKind)
        ? globalThis.String(object.channelKind)
        : isSet(object.channel_kind)
        ? globalThis.String(object.channel_kind)
        : "",
      position: isSet(object.position) ? globalThis.Number(object.position) : 0,
      unreadCount: isSet(object.unreadCount)
        ? globalThis.Number(object.unreadCount)
        : isSet(object.unread_count)
        ? globalThis.Number(object.unread_count)
        : 0,
    };
  },

  toJSON(message: WorkspaceBootstrapChannel): unknown {
    const obj: any = {};
    if (message.channelId !== "") {
      obj.channelId = message.channelId;
    }
    if (message.conversationId !== "") {
      obj.conversationId = message.conversationId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.channelKind !== "") {
      obj.channelKind = message.channelKind;
    }
    if (message.position !== 0) {
      obj.position = Math.round(message.position);
    }
    if (message.unreadCount !== 0) {
      obj.unreadCount = Math.round(message.unreadCount);
    }
    return obj;
  },

  create(base?: DeepPartial<WorkspaceBootstrapChannel>): WorkspaceBootstrapChannel {
    return WorkspaceBootstrapChannel.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WorkspaceBootstrapChannel>): WorkspaceBootstrapChannel {
    const message = createBaseWorkspaceBootstrapChannel();
    message.channelId = object.channelId ?? "";
    message.conversationId = object.conversationId ?? "";
    message.name = object.name ?? "";
    message.channelKind = object.channelKind ?? "";
    message.position = object.position ?? 0;
    message.unreadCount = object.unreadCount ?? 0;
    return message;
  },
};

function createBaseGetWorkspaceBootstrapRequest(): GetWorkspaceBootstrapRequest {
  return { workspaceId: "" };
}

export const GetWorkspaceBootstrapRequest: MessageFns<GetWorkspaceBootstrapRequest> = {
  encode(message: GetWorkspaceBootstrapRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspaceId !== "") {
      writer.uint32(10).string(message.workspaceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetWorkspaceBootstrapRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetWorkspaceBootstrapRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspaceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetWorkspaceBootstrapRequest {
    return {
      workspaceId: isSet(object.workspaceId)
        ? globalThis.String(object.workspaceId)
        : isSet(object.workspace_id)
        ? globalThis.String(object.workspace_id)
        : "",
    };
  },

  toJSON(message: GetWorkspaceBootstrapRequest): unknown {
    const obj: any = {};
    if (message.workspaceId !== "") {
      obj.workspaceId = message.workspaceId;
    }
    return obj;
  },

  create(base?: DeepPartial<GetWorkspaceBootstrapRequest>): GetWorkspaceBootstrapRequest {
    return GetWorkspaceBootstrapRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetWorkspaceBootstrapRequest>): GetWorkspaceBootstrapRequest {
    const message = createBaseGetWorkspaceBootstrapRequest();
    message.workspaceId = object.workspaceId ?? "";
    return message;
  },
};

function createBaseGetWorkspaceBootstrapResponse(): GetWorkspaceBootstrapResponse {
  return { workspace: undefined, channels: [] };
}

export const GetWorkspaceBootstrapResponse: MessageFns<GetWorkspaceBootstrapResponse> = {
  encode(message: GetWorkspaceBootstrapResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.workspace !== undefined) {
      WorkspaceBootstrapHeader.encode(message.workspace, writer.uint32(10).fork()).join();
    }
    for (const v of message.channels) {
      WorkspaceBootstrapChannel.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetWorkspaceBootstrapResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetWorkspaceBootstrapResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.workspace = WorkspaceBootstrapHeader.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.channels.push(WorkspaceBootstrapChannel.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetWorkspaceBootstrapResponse {
    return {
      workspace: isSet(object.workspace) ? WorkspaceBootstrapHeader.fromJSON(object.workspace) : undefined,
      channels: globalThis.Array.isArray(object?.channels)
        ? object.channels.map((e: any) => WorkspaceBootstrapChannel.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetWorkspaceBootstrapResponse): unknown {
    const obj: any = {};
    if (message.workspace !== undefined) {
      obj.workspace = WorkspaceBootstrapHeader.toJSON(message.workspace);
    }
    if (message.channels?.length) {
      obj.channels = message.channels.map((e) => WorkspaceBootstrapChannel.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetWorkspaceBootstrapResponse>): GetWorkspaceBootstrapResponse {
    return GetWorkspaceBootstrapResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetWorkspaceBootstrapResponse>): GetWorkspaceBootstrapResponse {
    const message = createBaseGetWorkspaceBootstrapResponse();
    message.workspace = (object.workspace !== undefined && object.workspace !== null)
      ? WorkspaceBootstrapHeader.fromPartial(object.workspace)
      : undefined;
    message.channels = object.channels?.map((e) => WorkspaceBootstrapChannel.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDmThreadItem(): DmThreadItem {
  return {
    conversationId: "",
    dmPairId: "",
    peerUserId: "",
    peerUsername: "",
    peerDisplayName: "",
    peerAvatarUrl: undefined,
    unreadCount: 0,
    lastMessagePreview: "",
    lastActivityAt: undefined,
  };
}

export const DmThreadItem: MessageFns<DmThreadItem> = {
  encode(message: DmThreadItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.conversationId !== "") {
      writer.uint32(10).string(message.conversationId);
    }
    if (message.dmPairId !== "") {
      writer.uint32(18).string(message.dmPairId);
    }
    if (message.peerUserId !== "") {
      writer.uint32(26).string(message.peerUserId);
    }
    if (message.peerUsername !== "") {
      writer.uint32(34).string(message.peerUsername);
    }
    if (message.peerDisplayName !== "") {
      writer.uint32(42).string(message.peerDisplayName);
    }
    if (message.peerAvatarUrl !== undefined) {
      writer.uint32(50).string(message.peerAvatarUrl);
    }
    if (message.unreadCount !== 0) {
      writer.uint32(56).int32(message.unreadCount);
    }
    if (message.lastMessagePreview !== "") {
      writer.uint32(66).string(message.lastMessagePreview);
    }
    if (message.lastActivityAt !== undefined) {
      Timestamp.encode(toTimestamp(message.lastActivityAt), writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DmThreadItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDmThreadItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.conversationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dmPairId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.peerUserId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.peerUsername = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.peerDisplayName = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.peerAvatarUrl = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.unreadCount = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.lastMessagePreview = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.lastActivityAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DmThreadItem {
    return {
      conversationId: isSet(object.conversationId)
        ? globalThis.String(object.conversationId)
        : isSet(object.conversation_id)
        ? globalThis.String(object.conversation_id)
        : "",
      dmPairId: isSet(object.dmPairId)
        ? globalThis.String(object.dmPairId)
        : isSet(object.dm_pair_id)
        ? globalThis.String(object.dm_pair_id)
        : "",
      peerUserId: isSet(object.peerUserId)
        ? globalThis.String(object.peerUserId)
        : isSet(object.peer_user_id)
        ? globalThis.String(object.peer_user_id)
        : "",
      peerUsername: isSet(object.peerUsername)
        ? globalThis.String(object.peerUsername)
        : isSet(object.peer_username)
        ? globalThis.String(object.peer_username)
        : "",
      peerDisplayName: isSet(object.peerDisplayName)
        ? globalThis.String(object.peerDisplayName)
        : isSet(object.peer_display_name)
        ? globalThis.String(object.peer_display_name)
        : "",
      peerAvatarUrl: isSet(object.peerAvatarUrl)
        ? globalThis.String(object.peerAvatarUrl)
        : isSet(object.peer_avatar_url)
        ? globalThis.String(object.peer_avatar_url)
        : undefined,
      unreadCount: isSet(object.unreadCount)
        ? globalThis.Number(object.unreadCount)
        : isSet(object.unread_count)
        ? globalThis.Number(object.unread_count)
        : 0,
      lastMessagePreview: isSet(object.lastMessagePreview)
        ? globalThis.String(object.lastMessagePreview)
        : isSet(object.last_message_preview)
        ? globalThis.String(object.last_message_preview)
        : "",
      lastActivityAt: isSet(object.lastActivityAt)
        ? fromJsonTimestamp(object.lastActivityAt)
        : isSet(object.last_activity_at)
        ? fromJsonTimestamp(object.last_activity_at)
        : undefined,
    };
  },

  toJSON(message: DmThreadItem): unknown {
    const obj: any = {};
    if (message.conversationId !== "") {
      obj.conversationId = message.conversationId;
    }
    if (message.dmPairId !== "") {
      obj.dmPairId = message.dmPairId;
    }
    if (message.peerUserId !== "") {
      obj.peerUserId = message.peerUserId;
    }
    if (message.peerUsername !== "") {
      obj.peerUsername = message.peerUsername;
    }
    if (message.peerDisplayName !== "") {
      obj.peerDisplayName = message.peerDisplayName;
    }
    if (message.peerAvatarUrl !== undefined) {
      obj.peerAvatarUrl = message.peerAvatarUrl;
    }
    if (message.unreadCount !== 0) {
      obj.unreadCount = Math.round(message.unreadCount);
    }
    if (message.lastMessagePreview !== "") {
      obj.lastMessagePreview = message.lastMessagePreview;
    }
    if (message.lastActivityAt !== undefined) {
      obj.lastActivityAt = message.lastActivityAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<DmThreadItem>): DmThreadItem {
    return DmThreadItem.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DmThreadItem>): DmThreadItem {
    const message = createBaseDmThreadItem();
    message.conversationId = object.conversationId ?? "";
    message.dmPairId = object.dmPairId ?? "";
    message.peerUserId = object.peerUserId ?? "";
    message.peerUsername = object.peerUsername ?? "";
    message.peerDisplayName = object.peerDisplayName ?? "";
    message.peerAvatarUrl = object.peerAvatarUrl ?? undefined;
    message.unreadCount = object.unreadCount ?? 0;
    message.lastMessagePreview = object.lastMessagePreview ?? "";
    message.lastActivityAt = object.lastActivityAt ?? undefined;
    return message;
  },
};

function createBaseGetDmBootstrapRequest(): GetDmBootstrapRequest {
  return {};
}

export const GetDmBootstrapRequest: MessageFns<GetDmBootstrapRequest> = {
  encode(_: GetDmBootstrapRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetDmBootstrapRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDmBootstrapRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetDmBootstrapRequest {
    return {};
  },

  toJSON(_: GetDmBootstrapRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetDmBootstrapRequest>): GetDmBootstrapRequest {
    return GetDmBootstrapRequest.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetDmBootstrapRequest>): GetDmBootstrapRequest {
    const message = createBaseGetDmBootstrapRequest();
    return message;
  },
};

function createBaseGetDmBootstrapResponse(): GetDmBootstrapResponse {
  return { items: [] };
}

export const GetDmBootstrapResponse: MessageFns<GetDmBootstrapResponse> = {
  encode(message: GetDmBootstrapResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      DmThreadItem.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetDmBootstrapResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDmBootstrapResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(DmThreadItem.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDmBootstrapResponse {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => DmThreadItem.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetDmBootstrapResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => DmThreadItem.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetDmBootstrapResponse>): GetDmBootstrapResponse {
    return GetDmBootstrapResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetDmBootstrapResponse>): GetDmBootstrapResponse {
    const message = createBaseGetDmBootstrapResponse();
    message.items = object.items?.map((e) => DmThreadItem.fromPartial(e)) || [];
    return message;
  },
};

export type BootstrapServiceDefinition = typeof BootstrapServiceDefinition;
export const BootstrapServiceDefinition = {
  name: "BootstrapService",
  fullName: "relay.bootstrap.BootstrapService",
  methods: {
    getAppBootstrap: {
      name: "GetAppBootstrap",
      requestType: GetAppBootstrapRequest as typeof GetAppBootstrapRequest,
      requestStream: false,
      responseType: GetAppBootstrapResponse as typeof GetAppBootstrapResponse,
      responseStream: false,
      options: {},
    },
    getWorkspaceBootstrap: {
      name: "GetWorkspaceBootstrap",
      requestType: GetWorkspaceBootstrapRequest as typeof GetWorkspaceBootstrapRequest,
      requestStream: false,
      responseType: GetWorkspaceBootstrapResponse as typeof GetWorkspaceBootstrapResponse,
      responseStream: false,
      options: {},
    },
    getDmBootstrap: {
      name: "GetDmBootstrap",
      requestType: GetDmBootstrapRequest as typeof GetDmBootstrapRequest,
      requestStream: false,
      responseType: GetDmBootstrapResponse as typeof GetDmBootstrapResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface BootstrapServiceImplementation<CallContextExt = {}> {
  getAppBootstrap(
    request: GetAppBootstrapRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetAppBootstrapResponse>>;
  getWorkspaceBootstrap(
    request: GetWorkspaceBootstrapRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetWorkspaceBootstrapResponse>>;
  getDmBootstrap(
    request: GetDmBootstrapRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetDmBootstrapResponse>>;
}

export interface BootstrapServiceClient<CallOptionsExt = {}> {
  getAppBootstrap(
    request: DeepPartial<GetAppBootstrapRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetAppBootstrapResponse>;
  getWorkspaceBootstrap(
    request: DeepPartial<GetWorkspaceBootstrapRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetWorkspaceBootstrapResponse>;
  getDmBootstrap(
    request: DeepPartial<GetDmBootstrapRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetDmBootstrapResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
