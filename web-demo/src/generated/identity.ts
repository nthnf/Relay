// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v3.19.1
// source: identity.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "relay.identity";

export interface UserProfile {
  userId: string;
  username: string;
  displayName: string;
  avatarUrl?: string | undefined;
}

export interface RegisterUserRequest {
  email: string;
  password: string;
  username: string;
  displayName: string;
  avatarUrl?: string | undefined;
}

export interface RegisterUserResponse {
  userId: string;
  email: string;
  verificationEmailRequestedAt: Date | undefined;
}

export interface AuthenticatePasswordRequest {
  email: string;
  password: string;
  clientInstanceId?: string | undefined;
}

export interface RefreshSessionRequest {
  refreshToken: string;
  clientInstanceId?: string | undefined;
}

export interface TokenPairResponse {
  userId: string;
  sessionId: string;
  accessToken: string;
  accessTokenExpiresAt: Date | undefined;
  refreshToken: string;
  refreshTokenExpiresAt: Date | undefined;
  emailVerified: boolean;
  profile: UserProfile | undefined;
}

export interface RevokeSessionRequest {
  sessionId: string;
  revokeReason?: string | undefined;
}

export interface RevokeSessionResponse {
  revoked: boolean;
  revokedAt?: Date | undefined;
}

export interface RedeemEmailVerificationTokenRequest {
  token: string;
}

export interface ResendVerificationEmailRequest {
  email: string;
}

export interface ResendVerificationEmailResponse {
  accepted: boolean;
}

export interface UpdateUserProfileRequest {
  displayName: string;
  avatarUrl?: string | undefined;
}

export interface UpdateUserProfileResponse {
  userId: string;
  username: string;
  displayName: string;
  avatarUrl?: string | undefined;
  updatedAt: Date | undefined;
}

export interface GetUserProfileRequest {
  userId?: string | undefined;
}

export interface GetUserProfileResponse {
  userId: string;
  username: string;
  displayName: string;
  avatarUrl?: string | undefined;
}

export interface GetUsersByIdsRequest {
  userIds: string[];
}

export interface GetUsersByIdsResponse {
  users: UserProfile[];
}

function createBaseUserProfile(): UserProfile {
  return { userId: "", username: "", displayName: "", avatarUrl: undefined };
}

export const UserProfile: MessageFns<UserProfile> = {
  encode(message: UserProfile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.displayName !== "") {
      writer.uint32(26).string(message.displayName);
    }
    if (message.avatarUrl !== undefined) {
      writer.uint32(34).string(message.avatarUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserProfile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserProfile();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.avatarUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserProfile {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      avatarUrl: isSet(object.avatarUrl)
        ? globalThis.String(object.avatarUrl)
        : isSet(object.avatar_url)
        ? globalThis.String(object.avatar_url)
        : undefined,
    };
  },

  toJSON(message: UserProfile): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.avatarUrl !== undefined) {
      obj.avatarUrl = message.avatarUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<UserProfile>): UserProfile {
    return UserProfile.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UserProfile>): UserProfile {
    const message = createBaseUserProfile();
    message.userId = object.userId ?? "";
    message.username = object.username ?? "";
    message.displayName = object.displayName ?? "";
    message.avatarUrl = object.avatarUrl ?? undefined;
    return message;
  },
};

function createBaseRegisterUserRequest(): RegisterUserRequest {
  return { email: "", password: "", username: "", displayName: "", avatarUrl: undefined };
}

export const RegisterUserRequest: MessageFns<RegisterUserRequest> = {
  encode(message: RegisterUserRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.password !== "") {
      writer.uint32(18).string(message.password);
    }
    if (message.username !== "") {
      writer.uint32(26).string(message.username);
    }
    if (message.displayName !== "") {
      writer.uint32(34).string(message.displayName);
    }
    if (message.avatarUrl !== undefined) {
      writer.uint32(42).string(message.avatarUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterUserRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterUserRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.avatarUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterUserRequest {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      avatarUrl: isSet(object.avatarUrl)
        ? globalThis.String(object.avatarUrl)
        : isSet(object.avatar_url)
        ? globalThis.String(object.avatar_url)
        : undefined,
    };
  },

  toJSON(message: RegisterUserRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.avatarUrl !== undefined) {
      obj.avatarUrl = message.avatarUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterUserRequest>): RegisterUserRequest {
    return RegisterUserRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterUserRequest>): RegisterUserRequest {
    const message = createBaseRegisterUserRequest();
    message.email = object.email ?? "";
    message.password = object.password ?? "";
    message.username = object.username ?? "";
    message.displayName = object.displayName ?? "";
    message.avatarUrl = object.avatarUrl ?? undefined;
    return message;
  },
};

function createBaseRegisterUserResponse(): RegisterUserResponse {
  return { userId: "", email: "", verificationEmailRequestedAt: undefined };
}

export const RegisterUserResponse: MessageFns<RegisterUserResponse> = {
  encode(message: RegisterUserResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.email !== "") {
      writer.uint32(18).string(message.email);
    }
    if (message.verificationEmailRequestedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.verificationEmailRequestedAt), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterUserResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterUserResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.verificationEmailRequestedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterUserResponse {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      verificationEmailRequestedAt: isSet(object.verificationEmailRequestedAt)
        ? fromJsonTimestamp(object.verificationEmailRequestedAt)
        : isSet(object.verification_email_requested_at)
        ? fromJsonTimestamp(object.verification_email_requested_at)
        : undefined,
    };
  },

  toJSON(message: RegisterUserResponse): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.verificationEmailRequestedAt !== undefined) {
      obj.verificationEmailRequestedAt = message.verificationEmailRequestedAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<RegisterUserResponse>): RegisterUserResponse {
    return RegisterUserResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RegisterUserResponse>): RegisterUserResponse {
    const message = createBaseRegisterUserResponse();
    message.userId = object.userId ?? "";
    message.email = object.email ?? "";
    message.verificationEmailRequestedAt = object.verificationEmailRequestedAt ?? undefined;
    return message;
  },
};

function createBaseAuthenticatePasswordRequest(): AuthenticatePasswordRequest {
  return { email: "", password: "", clientInstanceId: undefined };
}

export const AuthenticatePasswordRequest: MessageFns<AuthenticatePasswordRequest> = {
  encode(message: AuthenticatePasswordRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.password !== "") {
      writer.uint32(18).string(message.password);
    }
    if (message.clientInstanceId !== undefined) {
      writer.uint32(26).string(message.clientInstanceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthenticatePasswordRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthenticatePasswordRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.clientInstanceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthenticatePasswordRequest {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
      clientInstanceId: isSet(object.clientInstanceId)
        ? globalThis.String(object.clientInstanceId)
        : isSet(object.client_instance_id)
        ? globalThis.String(object.client_instance_id)
        : undefined,
    };
  },

  toJSON(message: AuthenticatePasswordRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    if (message.clientInstanceId !== undefined) {
      obj.clientInstanceId = message.clientInstanceId;
    }
    return obj;
  },

  create(base?: DeepPartial<AuthenticatePasswordRequest>): AuthenticatePasswordRequest {
    return AuthenticatePasswordRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthenticatePasswordRequest>): AuthenticatePasswordRequest {
    const message = createBaseAuthenticatePasswordRequest();
    message.email = object.email ?? "";
    message.password = object.password ?? "";
    message.clientInstanceId = object.clientInstanceId ?? undefined;
    return message;
  },
};

function createBaseRefreshSessionRequest(): RefreshSessionRequest {
  return { refreshToken: "", clientInstanceId: undefined };
}

export const RefreshSessionRequest: MessageFns<RefreshSessionRequest> = {
  encode(message: RefreshSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.refreshToken !== "") {
      writer.uint32(10).string(message.refreshToken);
    }
    if (message.clientInstanceId !== undefined) {
      writer.uint32(18).string(message.clientInstanceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RefreshSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRefreshSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.refreshToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clientInstanceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RefreshSessionRequest {
    return {
      refreshToken: isSet(object.refreshToken)
        ? globalThis.String(object.refreshToken)
        : isSet(object.refresh_token)
        ? globalThis.String(object.refresh_token)
        : "",
      clientInstanceId: isSet(object.clientInstanceId)
        ? globalThis.String(object.clientInstanceId)
        : isSet(object.client_instance_id)
        ? globalThis.String(object.client_instance_id)
        : undefined,
    };
  },

  toJSON(message: RefreshSessionRequest): unknown {
    const obj: any = {};
    if (message.refreshToken !== "") {
      obj.refreshToken = message.refreshToken;
    }
    if (message.clientInstanceId !== undefined) {
      obj.clientInstanceId = message.clientInstanceId;
    }
    return obj;
  },

  create(base?: DeepPartial<RefreshSessionRequest>): RefreshSessionRequest {
    return RefreshSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RefreshSessionRequest>): RefreshSessionRequest {
    const message = createBaseRefreshSessionRequest();
    message.refreshToken = object.refreshToken ?? "";
    message.clientInstanceId = object.clientInstanceId ?? undefined;
    return message;
  },
};

function createBaseTokenPairResponse(): TokenPairResponse {
  return {
    userId: "",
    sessionId: "",
    accessToken: "",
    accessTokenExpiresAt: undefined,
    refreshToken: "",
    refreshTokenExpiresAt: undefined,
    emailVerified: false,
    profile: undefined,
  };
}

export const TokenPairResponse: MessageFns<TokenPairResponse> = {
  encode(message: TokenPairResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.sessionId !== "") {
      writer.uint32(18).string(message.sessionId);
    }
    if (message.accessToken !== "") {
      writer.uint32(26).string(message.accessToken);
    }
    if (message.accessTokenExpiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.accessTokenExpiresAt), writer.uint32(34).fork()).join();
    }
    if (message.refreshToken !== "") {
      writer.uint32(42).string(message.refreshToken);
    }
    if (message.refreshTokenExpiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.refreshTokenExpiresAt), writer.uint32(50).fork()).join();
    }
    if (message.emailVerified !== false) {
      writer.uint32(56).bool(message.emailVerified);
    }
    if (message.profile !== undefined) {
      UserProfile.encode(message.profile, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TokenPairResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTokenPairResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.accessToken = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.accessTokenExpiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.refreshToken = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.refreshTokenExpiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.emailVerified = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.profile = UserProfile.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TokenPairResponse {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      accessToken: isSet(object.accessToken)
        ? globalThis.String(object.accessToken)
        : isSet(object.access_token)
        ? globalThis.String(object.access_token)
        : "",
      accessTokenExpiresAt: isSet(object.accessTokenExpiresAt)
        ? fromJsonTimestamp(object.accessTokenExpiresAt)
        : isSet(object.access_token_expires_at)
        ? fromJsonTimestamp(object.access_token_expires_at)
        : undefined,
      refreshToken: isSet(object.refreshToken)
        ? globalThis.String(object.refreshToken)
        : isSet(object.refresh_token)
        ? globalThis.String(object.refresh_token)
        : "",
      refreshTokenExpiresAt: isSet(object.refreshTokenExpiresAt)
        ? fromJsonTimestamp(object.refreshTokenExpiresAt)
        : isSet(object.refresh_token_expires_at)
        ? fromJsonTimestamp(object.refresh_token_expires_at)
        : undefined,
      emailVerified: isSet(object.emailVerified)
        ? globalThis.Boolean(object.emailVerified)
        : isSet(object.email_verified)
        ? globalThis.Boolean(object.email_verified)
        : false,
      profile: isSet(object.profile) ? UserProfile.fromJSON(object.profile) : undefined,
    };
  },

  toJSON(message: TokenPairResponse): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.accessToken !== "") {
      obj.accessToken = message.accessToken;
    }
    if (message.accessTokenExpiresAt !== undefined) {
      obj.accessTokenExpiresAt = message.accessTokenExpiresAt.toISOString();
    }
    if (message.refreshToken !== "") {
      obj.refreshToken = message.refreshToken;
    }
    if (message.refreshTokenExpiresAt !== undefined) {
      obj.refreshTokenExpiresAt = message.refreshTokenExpiresAt.toISOString();
    }
    if (message.emailVerified !== false) {
      obj.emailVerified = message.emailVerified;
    }
    if (message.profile !== undefined) {
      obj.profile = UserProfile.toJSON(message.profile);
    }
    return obj;
  },

  create(base?: DeepPartial<TokenPairResponse>): TokenPairResponse {
    return TokenPairResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TokenPairResponse>): TokenPairResponse {
    const message = createBaseTokenPairResponse();
    message.userId = object.userId ?? "";
    message.sessionId = object.sessionId ?? "";
    message.accessToken = object.accessToken ?? "";
    message.accessTokenExpiresAt = object.accessTokenExpiresAt ?? undefined;
    message.refreshToken = object.refreshToken ?? "";
    message.refreshTokenExpiresAt = object.refreshTokenExpiresAt ?? undefined;
    message.emailVerified = object.emailVerified ?? false;
    message.profile = (object.profile !== undefined && object.profile !== null)
      ? UserProfile.fromPartial(object.profile)
      : undefined;
    return message;
  },
};

function createBaseRevokeSessionRequest(): RevokeSessionRequest {
  return { sessionId: "", revokeReason: undefined };
}

export const RevokeSessionRequest: MessageFns<RevokeSessionRequest> = {
  encode(message: RevokeSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.revokeReason !== undefined) {
      writer.uint32(18).string(message.revokeReason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevokeSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevokeSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.revokeReason = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RevokeSessionRequest {
    return {
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      revokeReason: isSet(object.revokeReason)
        ? globalThis.String(object.revokeReason)
        : isSet(object.revoke_reason)
        ? globalThis.String(object.revoke_reason)
        : undefined,
    };
  },

  toJSON(message: RevokeSessionRequest): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.revokeReason !== undefined) {
      obj.revokeReason = message.revokeReason;
    }
    return obj;
  },

  create(base?: DeepPartial<RevokeSessionRequest>): RevokeSessionRequest {
    return RevokeSessionRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RevokeSessionRequest>): RevokeSessionRequest {
    const message = createBaseRevokeSessionRequest();
    message.sessionId = object.sessionId ?? "";
    message.revokeReason = object.revokeReason ?? undefined;
    return message;
  },
};

function createBaseRevokeSessionResponse(): RevokeSessionResponse {
  return { revoked: false, revokedAt: undefined };
}

export const RevokeSessionResponse: MessageFns<RevokeSessionResponse> = {
  encode(message: RevokeSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.revoked !== false) {
      writer.uint32(8).bool(message.revoked);
    }
    if (message.revokedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.revokedAt), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevokeSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevokeSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.revoked = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.revokedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RevokeSessionResponse {
    return {
      revoked: isSet(object.revoked) ? globalThis.Boolean(object.revoked) : false,
      revokedAt: isSet(object.revokedAt)
        ? fromJsonTimestamp(object.revokedAt)
        : isSet(object.revoked_at)
        ? fromJsonTimestamp(object.revoked_at)
        : undefined,
    };
  },

  toJSON(message: RevokeSessionResponse): unknown {
    const obj: any = {};
    if (message.revoked !== false) {
      obj.revoked = message.revoked;
    }
    if (message.revokedAt !== undefined) {
      obj.revokedAt = message.revokedAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<RevokeSessionResponse>): RevokeSessionResponse {
    return RevokeSessionResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RevokeSessionResponse>): RevokeSessionResponse {
    const message = createBaseRevokeSessionResponse();
    message.revoked = object.revoked ?? false;
    message.revokedAt = object.revokedAt ?? undefined;
    return message;
  },
};

function createBaseRedeemEmailVerificationTokenRequest(): RedeemEmailVerificationTokenRequest {
  return { token: "" };
}

export const RedeemEmailVerificationTokenRequest: MessageFns<RedeemEmailVerificationTokenRequest> = {
  encode(message: RedeemEmailVerificationTokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RedeemEmailVerificationTokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRedeemEmailVerificationTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RedeemEmailVerificationTokenRequest {
    return { token: isSet(object.token) ? globalThis.String(object.token) : "" };
  },

  toJSON(message: RedeemEmailVerificationTokenRequest): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create(base?: DeepPartial<RedeemEmailVerificationTokenRequest>): RedeemEmailVerificationTokenRequest {
    return RedeemEmailVerificationTokenRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RedeemEmailVerificationTokenRequest>): RedeemEmailVerificationTokenRequest {
    const message = createBaseRedeemEmailVerificationTokenRequest();
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseResendVerificationEmailRequest(): ResendVerificationEmailRequest {
  return { email: "" };
}

export const ResendVerificationEmailRequest: MessageFns<ResendVerificationEmailRequest> = {
  encode(message: ResendVerificationEmailRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResendVerificationEmailRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResendVerificationEmailRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResendVerificationEmailRequest {
    return { email: isSet(object.email) ? globalThis.String(object.email) : "" };
  },

  toJSON(message: ResendVerificationEmailRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    return obj;
  },

  create(base?: DeepPartial<ResendVerificationEmailRequest>): ResendVerificationEmailRequest {
    return ResendVerificationEmailRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ResendVerificationEmailRequest>): ResendVerificationEmailRequest {
    const message = createBaseResendVerificationEmailRequest();
    message.email = object.email ?? "";
    return message;
  },
};

function createBaseResendVerificationEmailResponse(): ResendVerificationEmailResponse {
  return { accepted: false };
}

export const ResendVerificationEmailResponse: MessageFns<ResendVerificationEmailResponse> = {
  encode(message: ResendVerificationEmailResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accepted !== false) {
      writer.uint32(8).bool(message.accepted);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ResendVerificationEmailResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResendVerificationEmailResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accepted = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ResendVerificationEmailResponse {
    return { accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false };
  },

  toJSON(message: ResendVerificationEmailResponse): unknown {
    const obj: any = {};
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    return obj;
  },

  create(base?: DeepPartial<ResendVerificationEmailResponse>): ResendVerificationEmailResponse {
    return ResendVerificationEmailResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ResendVerificationEmailResponse>): ResendVerificationEmailResponse {
    const message = createBaseResendVerificationEmailResponse();
    message.accepted = object.accepted ?? false;
    return message;
  },
};

function createBaseUpdateUserProfileRequest(): UpdateUserProfileRequest {
  return { displayName: "", avatarUrl: undefined };
}

export const UpdateUserProfileRequest: MessageFns<UpdateUserProfileRequest> = {
  encode(message: UpdateUserProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.displayName !== "") {
      writer.uint32(10).string(message.displayName);
    }
    if (message.avatarUrl !== undefined) {
      writer.uint32(18).string(message.avatarUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateUserProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateUserProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.avatarUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateUserProfileRequest {
    return {
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      avatarUrl: isSet(object.avatarUrl)
        ? globalThis.String(object.avatarUrl)
        : isSet(object.avatar_url)
        ? globalThis.String(object.avatar_url)
        : undefined,
    };
  },

  toJSON(message: UpdateUserProfileRequest): unknown {
    const obj: any = {};
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.avatarUrl !== undefined) {
      obj.avatarUrl = message.avatarUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateUserProfileRequest>): UpdateUserProfileRequest {
    return UpdateUserProfileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateUserProfileRequest>): UpdateUserProfileRequest {
    const message = createBaseUpdateUserProfileRequest();
    message.displayName = object.displayName ?? "";
    message.avatarUrl = object.avatarUrl ?? undefined;
    return message;
  },
};

function createBaseUpdateUserProfileResponse(): UpdateUserProfileResponse {
  return { userId: "", username: "", displayName: "", avatarUrl: undefined, updatedAt: undefined };
}

export const UpdateUserProfileResponse: MessageFns<UpdateUserProfileResponse> = {
  encode(message: UpdateUserProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.displayName !== "") {
      writer.uint32(26).string(message.displayName);
    }
    if (message.avatarUrl !== undefined) {
      writer.uint32(34).string(message.avatarUrl);
    }
    if (message.updatedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.updatedAt), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateUserProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateUserProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.avatarUrl = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.updatedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateUserProfileResponse {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      avatarUrl: isSet(object.avatarUrl)
        ? globalThis.String(object.avatarUrl)
        : isSet(object.avatar_url)
        ? globalThis.String(object.avatar_url)
        : undefined,
      updatedAt: isSet(object.updatedAt)
        ? fromJsonTimestamp(object.updatedAt)
        : isSet(object.updated_at)
        ? fromJsonTimestamp(object.updated_at)
        : undefined,
    };
  },

  toJSON(message: UpdateUserProfileResponse): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.avatarUrl !== undefined) {
      obj.avatarUrl = message.avatarUrl;
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateUserProfileResponse>): UpdateUserProfileResponse {
    return UpdateUserProfileResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateUserProfileResponse>): UpdateUserProfileResponse {
    const message = createBaseUpdateUserProfileResponse();
    message.userId = object.userId ?? "";
    message.username = object.username ?? "";
    message.displayName = object.displayName ?? "";
    message.avatarUrl = object.avatarUrl ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseGetUserProfileRequest(): GetUserProfileRequest {
  return { userId: undefined };
}

export const GetUserProfileRequest: MessageFns<GetUserProfileRequest> = {
  encode(message: GetUserProfileRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== undefined) {
      writer.uint32(10).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserProfileRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserProfileRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserProfileRequest {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : undefined,
    };
  },

  toJSON(message: GetUserProfileRequest): unknown {
    const obj: any = {};
    if (message.userId !== undefined) {
      obj.userId = message.userId;
    }
    return obj;
  },

  create(base?: DeepPartial<GetUserProfileRequest>): GetUserProfileRequest {
    return GetUserProfileRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUserProfileRequest>): GetUserProfileRequest {
    const message = createBaseGetUserProfileRequest();
    message.userId = object.userId ?? undefined;
    return message;
  },
};

function createBaseGetUserProfileResponse(): GetUserProfileResponse {
  return { userId: "", username: "", displayName: "", avatarUrl: undefined };
}

export const GetUserProfileResponse: MessageFns<GetUserProfileResponse> = {
  encode(message: GetUserProfileResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== "") {
      writer.uint32(10).string(message.userId);
    }
    if (message.username !== "") {
      writer.uint32(34).string(message.username);
    }
    if (message.displayName !== "") {
      writer.uint32(42).string(message.displayName);
    }
    if (message.avatarUrl !== undefined) {
      writer.uint32(50).string(message.avatarUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserProfileResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserProfileResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.avatarUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserProfileResponse {
    return {
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      avatarUrl: isSet(object.avatarUrl)
        ? globalThis.String(object.avatarUrl)
        : isSet(object.avatar_url)
        ? globalThis.String(object.avatar_url)
        : undefined,
    };
  },

  toJSON(message: GetUserProfileResponse): unknown {
    const obj: any = {};
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.avatarUrl !== undefined) {
      obj.avatarUrl = message.avatarUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<GetUserProfileResponse>): GetUserProfileResponse {
    return GetUserProfileResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUserProfileResponse>): GetUserProfileResponse {
    const message = createBaseGetUserProfileResponse();
    message.userId = object.userId ?? "";
    message.username = object.username ?? "";
    message.displayName = object.displayName ?? "";
    message.avatarUrl = object.avatarUrl ?? undefined;
    return message;
  },
};

function createBaseGetUsersByIdsRequest(): GetUsersByIdsRequest {
  return { userIds: [] };
}

export const GetUsersByIdsRequest: MessageFns<GetUsersByIdsRequest> = {
  encode(message: GetUsersByIdsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.userIds) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUsersByIdsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUsersByIdsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.userIds.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUsersByIdsRequest {
    return {
      userIds: globalThis.Array.isArray(object?.userIds)
        ? object.userIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.user_ids)
        ? object.user_ids.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: GetUsersByIdsRequest): unknown {
    const obj: any = {};
    if (message.userIds?.length) {
      obj.userIds = message.userIds;
    }
    return obj;
  },

  create(base?: DeepPartial<GetUsersByIdsRequest>): GetUsersByIdsRequest {
    return GetUsersByIdsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUsersByIdsRequest>): GetUsersByIdsRequest {
    const message = createBaseGetUsersByIdsRequest();
    message.userIds = object.userIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseGetUsersByIdsResponse(): GetUsersByIdsResponse {
  return { users: [] };
}

export const GetUsersByIdsResponse: MessageFns<GetUsersByIdsResponse> = {
  encode(message: GetUsersByIdsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.users) {
      UserProfile.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUsersByIdsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUsersByIdsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.users.push(UserProfile.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUsersByIdsResponse {
    return {
      users: globalThis.Array.isArray(object?.users) ? object.users.map((e: any) => UserProfile.fromJSON(e)) : [],
    };
  },

  toJSON(message: GetUsersByIdsResponse): unknown {
    const obj: any = {};
    if (message.users?.length) {
      obj.users = message.users.map((e) => UserProfile.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GetUsersByIdsResponse>): GetUsersByIdsResponse {
    return GetUsersByIdsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetUsersByIdsResponse>): GetUsersByIdsResponse {
    const message = createBaseGetUsersByIdsResponse();
    message.users = object.users?.map((e) => UserProfile.fromPartial(e)) || [];
    return message;
  },
};

export type IdentityServiceDefinition = typeof IdentityServiceDefinition;
export const IdentityServiceDefinition = {
  name: "IdentityService",
  fullName: "relay.identity.IdentityService",
  methods: {
    registerUser: {
      name: "RegisterUser",
      requestType: RegisterUserRequest as typeof RegisterUserRequest,
      requestStream: false,
      responseType: RegisterUserResponse as typeof RegisterUserResponse,
      responseStream: false,
      options: {},
    },
    authenticatePassword: {
      name: "AuthenticatePassword",
      requestType: AuthenticatePasswordRequest as typeof AuthenticatePasswordRequest,
      requestStream: false,
      responseType: TokenPairResponse as typeof TokenPairResponse,
      responseStream: false,
      options: {},
    },
    refreshSession: {
      name: "RefreshSession",
      requestType: RefreshSessionRequest as typeof RefreshSessionRequest,
      requestStream: false,
      responseType: TokenPairResponse as typeof TokenPairResponse,
      responseStream: false,
      options: {},
    },
    revokeSession: {
      name: "RevokeSession",
      requestType: RevokeSessionRequest as typeof RevokeSessionRequest,
      requestStream: false,
      responseType: RevokeSessionResponse as typeof RevokeSessionResponse,
      responseStream: false,
      options: {},
    },
    redeemEmailVerificationToken: {
      name: "RedeemEmailVerificationToken",
      requestType: RedeemEmailVerificationTokenRequest as typeof RedeemEmailVerificationTokenRequest,
      requestStream: false,
      responseType: TokenPairResponse as typeof TokenPairResponse,
      responseStream: false,
      options: {},
    },
    resendVerificationEmail: {
      name: "ResendVerificationEmail",
      requestType: ResendVerificationEmailRequest as typeof ResendVerificationEmailRequest,
      requestStream: false,
      responseType: ResendVerificationEmailResponse as typeof ResendVerificationEmailResponse,
      responseStream: false,
      options: {},
    },
    updateUserProfile: {
      name: "UpdateUserProfile",
      requestType: UpdateUserProfileRequest as typeof UpdateUserProfileRequest,
      requestStream: false,
      responseType: UpdateUserProfileResponse as typeof UpdateUserProfileResponse,
      responseStream: false,
      options: {},
    },
    getUserProfile: {
      name: "GetUserProfile",
      requestType: GetUserProfileRequest as typeof GetUserProfileRequest,
      requestStream: false,
      responseType: GetUserProfileResponse as typeof GetUserProfileResponse,
      responseStream: false,
      options: {},
    },
    getUsersByIds: {
      name: "GetUsersByIds",
      requestType: GetUsersByIdsRequest as typeof GetUsersByIdsRequest,
      requestStream: false,
      responseType: GetUsersByIdsResponse as typeof GetUsersByIdsResponse,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface IdentityServiceImplementation<CallContextExt = {}> {
  registerUser(
    request: RegisterUserRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<RegisterUserResponse>>;
  authenticatePassword(
    request: AuthenticatePasswordRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<TokenPairResponse>>;
  refreshSession(
    request: RefreshSessionRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<TokenPairResponse>>;
  revokeSession(
    request: RevokeSessionRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<RevokeSessionResponse>>;
  redeemEmailVerificationToken(
    request: RedeemEmailVerificationTokenRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<TokenPairResponse>>;
  resendVerificationEmail(
    request: ResendVerificationEmailRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<ResendVerificationEmailResponse>>;
  updateUserProfile(
    request: UpdateUserProfileRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<UpdateUserProfileResponse>>;
  getUserProfile(
    request: GetUserProfileRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetUserProfileResponse>>;
  getUsersByIds(
    request: GetUsersByIdsRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<GetUsersByIdsResponse>>;
}

export interface IdentityServiceClient<CallOptionsExt = {}> {
  registerUser(
    request: DeepPartial<RegisterUserRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<RegisterUserResponse>;
  authenticatePassword(
    request: DeepPartial<AuthenticatePasswordRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<TokenPairResponse>;
  refreshSession(
    request: DeepPartial<RefreshSessionRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<TokenPairResponse>;
  revokeSession(
    request: DeepPartial<RevokeSessionRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<RevokeSessionResponse>;
  redeemEmailVerificationToken(
    request: DeepPartial<RedeemEmailVerificationTokenRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<TokenPairResponse>;
  resendVerificationEmail(
    request: DeepPartial<ResendVerificationEmailRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<ResendVerificationEmailResponse>;
  updateUserProfile(
    request: DeepPartial<UpdateUserProfileRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<UpdateUserProfileResponse>;
  getUserProfile(
    request: DeepPartial<GetUserProfileRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetUserProfileResponse>;
  getUsersByIds(
    request: DeepPartial<GetUsersByIdsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<GetUsersByIdsResponse>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
